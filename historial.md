# Historial de Desarrollo - FleetSync AI

- **2026-03-02**: El proyecto ha sido inicializado. Se ha creado el entorno virtual de Python (`venv/`) y la estructura base de carpetas (`backend/`, `frontend/`, `mcp_server/`).
- **2026-03-02**: Se ha desarrollado el servidor MCP simulado en `mcp_server/server.py`. Este servidor expone tres herramientas que simulan APIs de CAMARA de Nokia (`check_number_verification`, `check_sim_swap`, `verify_location`) para su uso posterior por un agente de IA.
- **2026-03-02**: Se ha creado el cliente IA `backend/ai_agent.py`. Este módulo conecta con el MCP Server de forma asíncrona mediante `stdio_client`, ejecuta las herramientas de verificación, y procesa los resultados a través del modelo Gemini (con fallback integrado en caso de no disponer de API key) para retornar una evaluación en formato JSON indicando un status "SECURE" o "ALERT".
- **2026-03-02**: Se ha desarrollado el frontend en `frontend/app.py` utilizando Streamlit. La UI actúa como un Centro de Mando Logístico permitiendo la entrada de parámetros de telemetría, invocando al Agente de IA al pulsar un botón y renderizando el output JSON como banderas visuales (Verde SECURE, Rojo ALERT).
- **2026-03-02**: Se ha creado un script en la raíz `simulador.py`. Este script sirve para hacer una demo técnica ejecutando directamente llamadas al agente de IA con dos casos de uso: Escenario A (Situación normal donde el sistema reporta "SECURE") y Escenario B (Ataque donde el GPS no coincide con la red CAMARA, reportando "ALERT"). Se puede ejecutar la simulación desde consola usando `python simulador.py`.
- **2026-03-02**: Se han configurado medidas de seguridad y variables de entorno. Se crearon `.env` y `.gitignore` en la raíz. El script `backend/ai_agent.py` ha sido actualizado para cargar dichas variables usando `python-dotenv` (para claves como GEMINI_API_KEY o NOKIA_API_KEY) y se han incorporado bloques `try-except` robustos en la conexión e invocación del servidor MCP, garantizando que una caída del backend o de las herramientas no rompa el dashboard, reportando un estatus UNKNOWN de forma controlada.
- **2026-03-02**: Automatización de ejecución lista. Se ha creado el script `start.py` en la raíz del proyecto. Este orquestador ("Botón Rojo") automatiza la secuencia de arranque para los jueces del Hackathon, levantando en segundo plano la infraestructura MCP y ejecutando automáticamente el Dashboard de Streamlit en el puerto por defecto (8501).
- **2026-03-02**: Integración real con la API de Nokia Network as Code (NaC). El servidor `mcp_server/server.py` fue modificado para usar la librería `requests` y enviar peticiones reales al sandbox de Nokia (`/api/location-verification/v0/verify`). Se inyectó la clave Bearer desde la variable de entorno y se estructuró el payload con número, lat, lon y maxAge. Se implementó el manejo de errores estricto para retornar JSON descriptivo al AI si ocurre 401 (Unauthorized) o 500 (Server Error).
- **2026-03-02**: Ingeniería de System Prompt del Agente IA. Se ha inyectado una instrucción de sistema de grado militar en `backend/ai_agent.py` para anclar las decisiones de Gemini a los resultados de las herramientas Telco (especialmente la de red/NaC), asegurando que devuelva un estatus `ALERT` si existe discrepancia y mitigando alucinaciones. También se asegura la respuesta en formato JSON e inyecta los "network_logs" crudos al objeto devuelto para que el frontend los pueda consumir.
- **2026-03-02**: Puliendo la Demo Visual y de Rendimiento para el Jurado. La UI principal en `frontend/app.py` ha sido completamente rediseñada a layout 'wide' con dos columnas (`st.columns`). En la izquierda, los controles del simulador, y en la derecha, el log de la IA. Destaca un nuevo componente "Consola de Red" que expone el JSON real devuelto por la API de Nokia y, a continuación, el banner "estado seguro" o "alerta detectada" mostrando el verdadero poder de la integración visual a un juez.
- **2026-03-02**: Integración de Segunda API (QoD). Cumpliendo el requerimiento del Hackathon de múltiples APIs, el servidor `mcp_server/server.py` ahora expone la herramienta `activate_emergency_qod`. Además, el System Prompt en `backend/ai_agent.py` ha sido actualizado: Si la IA evalúa un secuestro (STATUS: ALERT), se gatilla mecánicamente una invocación a esta API para habilitar una "Autopista de Banda Ancha" (Quality of Service) para priorizar la transmisión de las cámaras de seguridad.
- **2026-03-02**: Módulo de Negocio y ROI para el Pitch. Actualizada la interfaz de Streamlit (`frontend/app.py`) con una cabecera financiera usando `st.metric`. Se añadieron métricas clave: "Valor de la Carga", "Costo Operativo API", y "Ahorro Estimado en Póliza". También se adjuntó un `st.line_chart` comparando incidentes (Hardware Tradicional vs FleetSync). Todo esto para blindar el criterio de Relevancia y Viabilidad Comercial frente al jurado.
- **2026-03-02**: Módulo de Negocio y ROI para el Pitch. Actualizada la interfaz de Streamlit (`frontend/app.py`) con una cabecera financiera usando `st.metric`. Se añadieron métricas clave: "Valor de la Carga", "Costo Operativo API", y "Ahorro Estimado en Póliza". También se adjuntó un `st.line_chart` comparando incidentes (Hardware Tradicional vs FleetSync). Todo esto para blindar el criterio de Relevancia y Viabilidad Comercial frente al jurado.
- **2026-03-02**: Revisión Final y QA. Repositorio consolidado (`requirements.txt` re-verificado con todas las dependencias necesarias: `streamlit`, `mcp`, `requests`, `pandas`, `numpy`, `python-dotenv`, `google-genai`). Añadidos comentarios extensos en `mcp_server/server.py` explicando el flujo de de las llamadas a Nokia NaC para que los jueces puedan leer el código con facilidad. Este archivo de historial ha sido formateado para fungir como el README técnico del proyecto. Todo listo para el pitch.
- **2026-03-02**: Fase de Documentación Oficial iniciada y completada. Se generó un robusto `README.md` en la raíz del proyecto que contiene todo el Technical Onboarding necesario para los jueces: Elevator Pitch, Arquitectura modularizada, Flujo del razonamiento de Ciberseguridad, e Instrucciones precisas de instalación y ejecución.
- **2026-03-02**: Fixes aplicados (Día 1): Eliminada la definición duplicada de `verify_location` en `mcp_server/server.py` y corregido el scope asíncrono de `activate_emergency_qod` en `backend/ai_agent.py` garantizando que se ejecute estrictamente dentro de `ClientSession`.
- **2026-03-02**: Flujo 1 (Autorización de Inicio de Ruta): Se desarrolló la nueva herramienta MCP `verify_truck_chip_location` en `mcp_server/server.py` que verifica si el chip del camión está en la ubicación indicada (simulando spoofing si el ID termina en '000'). En `backend/ai_agent.py` se implementó la función síncrona `start_journey` que solicita autorización al modelo de Gemini ejecutando 4 herramientas en paralelo: comprobación de validez de número, SIM swap, y las dos verificaciones de ubicación (Nokia NaC e IoT del camión) con un radio de 500m. Si alguna verificación falla, el inicio de ruta retorna "DENIED", en caso contrario "AUTHORIZED".
- **2026-03-02**: Flujo 2 (Herramienta Matemática para QoD): Se añadió la herramienta `calculate_distance` en `mcp_server/server.py` usando la fórmula de Haversine nativa de Python. En `backend/ai_agent.py`, se integró `activate_journey_qod` donde la IA toma decisiones autónomamente sobre la duración del QoD según la distancia (>50km=7200s, >20km=3600s), despachando una nueva llamada a `activate_emergency_qod` en cascada si es necesario.
- **2026-03-02**: Flujo 3 (Monitorización y Alertas): Se crearon estructuras en memoria temporales para el Hackathon en `backend/route_monitor.py` (`active_journeys = {}`) y `backend/incident_manager.py` (`incidents = []`). En `mcp_server/server.py` se implementaron las tools `check_route_deviation` para detectar salida de la ruta y `create_incident` para generar IDs únicos y persistir (en RAM) las incidencias con estado "OPEN".
- **2026-03-02**: Flujo 4 (Confirmación de Llegada): En `backend/ai_agent.py` se construyó el endpoint IA `confirm_arrival` que cierra el ciclo de vida del transporte logístico. Evaluando las simulaciones en paralelo (Nokia Location y Truck IoT Location) con 300m de radio más `calculate_distance`, Gemini dicta si se concreta el delivery ("ARRIVED"), detecta un robo selectivo donde solo llega el conductor pero no la carga ("ALERT" + incidente de "LOCATION_MISMATCH_AT_DESTINATION" + QoD de 1800s), o dictamina que el camión "NOT_ARRIVED". La integración full-stack finaliza publicando una nueva sección visual en el Dashboard Streamlit (`frontend/app.py`).
---

## 🏁 Conclusión del Hackathon y Resumen de Módulos

El desarrollo de la **V1.0 de FleetSync AI** ha concluido exitosamente cumpliendo con los estándares de integración técnica y viabilidad de negocio requeridos por el Open Gateway Hackathon 2026. 

A modo de recapitulación técnica, la arquitectura ha quedado solidificada en tres módulos autónomos y altamente acoplados a través del protocolo MCP:
1.  **Frontend Visual:** `frontend/app.py`. Centro de mando avanzado en Streamlit que orquesta la demo e inyecta soporte al pitch de negocio mediante métricas ROI.
2.  **Agente IA (Core):** `backend/ai_agent.py`. El cerebro que aplica heurísticas de ciberseguridad sobre la infraestructura Telco utilizando el razonamiento zero-shot de Gemini Pro.
3.  **MCP Server Telco:** `mcp_server/server.py`. Una pasarela certificada (construida bajo el Model Context Protocol) que expone las de Nokia Network as Code (Vía `requests` genuinas) para `verify_location` y `activate_emergency_qod`.

### 🚀 Next Steps (Post-Hackathon)

Para llevar este MVP de hackathon a una escala de Producción Empresarial, la hoja de ruta inmediata contempla:
1.  **Migración a Cloud (GCP):** Contenerizar los tres módulos en Docker y orquestarlos utilizando Google Kubernetes Engine (GKE) o Google Cloud Run para escalado elástico basado en tráfico de telemetría sincrónico.
2.  **Transporte SSE para MCP:** Migrar el `stdio_client` local actual a Server-Sent Events (SSE) para permitir que el MCP Server viva en un cluster separado y sea consumido de forma remota por el backend IA de Google.
3.  **Persistencia Transaccional:** Implementar Cloud SQL (PostgreSQL) para mantener un ledger inmutable de telemetría y disparos de la API QoD para auditorías policiales y de aseguradoras.

*V1.0 Completada para la evaluación del jurado.*

